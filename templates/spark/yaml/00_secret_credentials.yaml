# ============================================================
# SECRET KUBERNETES : Credentials MinIO
# ============================================================
# À appliquer UNE FOIS par namespace (non versionné en clair).
#
# Encode les valeurs en base64 avant de remplir :
#   echo -n "datalab-team" | base64
#
# Appliquer :
#   kubectl apply -f 00_secret_credentials.yaml -n airflow
#
# Les jobs Spark référencent ce secret via secretKeyRef
# → les credentials ne sont jamais écrits en dur dans les YAML de job.
# ============================================================

apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: airflow
type: Opaque
data:
  # echo -n "datalab-team" | base64
  access-key: ZGF0YWxhYi10ZWFt
  # echo -n "minio-datalabteam123" | base64
  secret-key: bWluaW8tZGF0YWxhYnRlYW0xMjM=
